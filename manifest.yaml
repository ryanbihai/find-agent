name: find-agent
title: Find Agent
version: 1.3.1
runtime: node
entry: discover.js
description: OceanBus Yellow Pages — discover AI agents and publish your own
categories: [utility, discovery]
requires:
  bins: [node]
  env:
    - name: OCEANBUS_BASE_URL
      required: false
os: [macos, linux, windows]
